java
```
curl https://gitee.com/smallsaas/sandbox/raw/master/tag/run/jar/java.sh -o /usr/bin/java
chmod +x /usr/bin/java
```
>

jar
```
curl https://gitee.com/smallsaas/sandbox/raw/master/tag/run/jar/java.sh -o /usr/bin/jar
chmod +x /usr/bin/jar
```
