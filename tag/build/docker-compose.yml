version: "3.4"
# COMPOSE_DOCKER_CLI_BUILD=1 docker-compose build
services:
  api:
    image: api:pom-build
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes: 
      - ./target:/webapps
    network_mode: host
