version: "3.4"
# COMPOSE_DOCKER_CLI_BUILD=1 DOCKER_BUILDKIT=1 docker-compose up --build
services:
  api:
    image: build-jar
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes: 
      - ./target:/webapps
    network_mode: host
